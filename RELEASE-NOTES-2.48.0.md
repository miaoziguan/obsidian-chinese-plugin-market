# v2.48.0 更新说明

> 一句话：**打开插件 / 搜索时的等待，从十几秒变成几乎秒开。**

## 你会感受到的变化

- **首屏几乎秒开**：此前每次打开都要在加载页停留十几秒，现在基本一闪而过
- **vault 里已采纳的译名终于生效了**：此前翻译记忆库里你确认过的中文译名一直没被读回来，卡片经常显示英文原文；本次修复后正常回灌
- **记忆库迁移不再卡顿**：旧目录搬迁改成分批并发，笔记多也不会卡住启动
- **加载提示更准确**：新增「正在加载插件统计数据…」阶段，不再一直挂着「正在合并离线翻译词典」让人以为卡死
- **启动不再拖慢 Obsidian**：向量索引改为后台加载，不再挡在首屏路径上

## 修了什么

- **记忆库目录从来没被真正创建**：一处判断漏写了 `await`，导致建目录的代码从未执行 —— 这是"启动慢"和"译名不生效"的**共同根因**
- **首屏不再空等统计数据最多 8 秒**：统计数据改为就绪即可用，不再被向量索引拖住（8 秒 → 0.3 秒）
- 向量索引归一化改为原地计算，省掉大量临时内存分配
- 写入笔记前自动创建父目录；记忆库目录不存在时不再报错中断

## 数据安全（请放心）

- 记忆库迁移是**先写后删**，原笔记进入 Obsidian 回收站，**不会丢失**
- 单条笔记迁移失败会自动跳过，下次启动继续重试
- 极端大库首次启动最多迁移 8 秒就先放行首屏，剩余部分下次启动接着搬（一般几秒就全部完成）

## 技术细节（给好奇的同学）

一个漏写的 `await` 引发的连锁反应：

```ts
if (!notes.exists(folderPath)) {   // exists 返回 Promise，没 await
  await notes.createFolder(folderPath);
}
```

`!Promise` 恒为 `false`，于是建目录分支**从未执行** → `.obsidian` 下的 `tm/` 目录从不创建 → 桌面端写入直接抛 `ENOENT` → 整条延迟初始化中断 → `scanVaultTM` 没跑 → `tmApprovedReady` 永不 resolve → 首屏死等 15 秒安全阀。

顺带还发现首屏会空转等 `cachedStats` 最多 8 秒（它的赋值被 `Promise.all` 卡在最慢的向量索引之后），一并改为「就绪即赋值」。

两层叠加，就是那"十几秒"的全部来源。

---

## 可直接复制的文案

### 社群 / 更新公告

> Obsidian 插件「中文区插件市场」更新到 v2.48.0：
>
> - 首屏从十几秒 → 几乎秒开
> - 修复：vault 里已采纳的译名一直没生效（卡片常显示英文原文）
> - 修复：记忆库目录从未被正确创建，导致每次启动都在空等
> - 迁移改为并发批处理，大库也不卡
> - 数据不丢：先写后删，原笔记进回收站
>
> 根因是一处漏写的 `await` —— 一句代码，十几秒等待。

### 朋友圈（短版）

> 给 Obsidian 插件修了个"十几秒启动"的问题。查到最后是一个漏写的 `await`：`!Promise` 恒为 false，建目录的代码从来没执行过，于是每次启动都在等一个永远不会 ready 的 Promise。改完：秒开。
>
> 教训是——性能优化先读日志，别猜。
