# 使用扁平（hoisted）node_modules 布局，等价于 npm 的扁平结构。
# 原因：esbuild.config.mjs 硬编码了 ./node_modules/onnxruntime-web 与 ./node_modules/sql.js 的
# 相对路径（WASM 资产拷贝 + transformers/ort 别名），而 onnxruntime-web 是 @huggingface/transformers
# 的间接依赖，pnpm 默认隔离布局不会把它提升到顶层 node_modules，导致 CI 构建报
# "Missing WASM asset"。pnpm-workspace.yaml 的 publicHoistPattern 亦是为扁平化而设。
node-linker=hoisted
